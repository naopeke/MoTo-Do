export type Todo = {
    item_id: string;
    description: string;
    isDone: boolean;
    order_index: number;
}

export type User = {
    user_id: string;
    name: string;
    email: string;
    password: string;
  };
  